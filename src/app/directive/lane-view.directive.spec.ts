import { LaneViewDirective } from './lane-view.directive';

describe('LaneViewDirective', () => {
  it('should create an instance', () => {
    const directive = new LaneViewDirective();
    expect(directive).toBeTruthy();
  });
});
